---
title: 'SocialSection'
description: 'Social media showcase grid with Instagram and Facebook integration'
---

## Overview

The `SocialSection` component displays a grid of social media posts that link to the brand's Instagram and Facebook pages. It creates an engaging visual showcase encouraging users to follow on social platforms.

## Import

```tsx
import { SocialSection } from '../components/SocialSection';
```

## Usage

```tsx
import { SocialSection } from './components/SocialSection';

function HomePage() {
  return (
    <>
      {/* Other sections */}
      <SocialSection /> {/* Social media showcase */}
      {/* More sections */}
    </>
  );
}
```

## Social Posts Data

The component defines an array of social post objects:

```tsx
const socialPosts = [
  {
    id: 1,
    image: "https://images.unsplash.com/photo-1516734212186-a967f81ad0d7?auto=format&fit=crop&q=80&w=400",
    link: "https://www.instagram.com/invites/contact/?i=1vlgoav5whto1&utm_content=myftgi8"
  },
  {
    id: 2,
    image: "https://images.unsplash.com/photo-1583337130417-3346a1be7dee?auto=format&fit=crop&q=80&w=400",
    link: "https://www.instagram.com/invites/contact/?i=1vlgoav5whto1&utm_content=myftgi8"
  },
  {
    id: 3,
    image: "https://images.unsplash.com/photo-1535930891776-0c2dfb7fda1a?auto=format&fit=crop&q=80&w=400",
    link: "https://www.instagram.com/invites/contact/?i=1vlgoav5whto1&utm_content=myftgi8"
  },
  {
    id: 4,
    image: "https://images.unsplash.com/photo-1541599540903-216a46ca1df0?auto=format&fit=crop&q=80&w=400",
    link: "https://www.instagram.com/invites/contact/?i=1vlgoav5whto1&utm_content=myftgi8"
  }
];
```

## Component Structure

<Steps>
  <Step title="Header">
    Section title, badge, and description
  </Step>
  <Step title="Post Grid">
    4 image cards linking to Instagram
  </Step>
  <Step title="Social Links">
    Facebook and Instagram handle links
  </Step>
</Steps>

## Section Header

```tsx
<div className="text-center mb-16" data-aos="fade-up">
  <span className="text-[#D4AF37] font-bold uppercase 
                   tracking-[0.3em] text-[10px] mb-4 block">
    Comunidad Jasper
  </span>
  
  <h2 className="text-5xl font-serif font-bold mb-6">
    Síguenos en Redes
  </h2>
  
  <p className="text-gray-500 max-w-xl mx-auto">
    Únete a nuestra comunidad en Instagram y Facebook para ver a 
    nuestros clientes felices y enterarte de las últimas novedades.
  </p>
</div>
```

<ParamField path="Badge" type="text">
  "Comunidad Jasper" in gold with extra wide tracking
</ParamField>

<ParamField path="Headline" type="text">
  "Síguenos en Redes" - Large serif font
</ParamField>

<ParamField path="Description" type="text">
  Invitation text centered with max-width constraint
</ParamField>

## Image Grid

The posts are displayed in a responsive grid:

```tsx
<div className="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-8">
  {socialPosts.map((post, i) => (
    <a
      key={post.id}
      href={post.link}
      target="_blank"
      rel="noopener noreferrer"
      className="relative aspect-square rounded-[32px] 
                 overflow-hidden group shadow-lg"
      data-aos="zoom-in"
      data-aos-delay={i * 100}
    >
      <img
        src={post.image}
        alt={`Social post ${post.id}`}
        className="w-full h-full object-cover transition-transform 
                   duration-700 group-hover:scale-110"
        referrerPolicy="no-referrer"
      />
      
      {/* Hover Overlay */}
      <div className="absolute inset-0 bg-black/40 opacity-0 
                      group-hover:opacity-100 transition-opacity 
                      flex items-center justify-center">
        <FontAwesomeIcon icon={faInstagram} 
                         className="text-white text-3xl" />
      </div>
    </a>
  ))}
</div>
```

### Grid Layout

<Tabs>
  <Tab title="Mobile">
    `grid-cols-2` - Two columns
    
    `gap-4` - 16px gap
  </Tab>
  <Tab title="Desktop (md+)">
    `md:grid-cols-4` - Four columns
    
    `md:gap-8` - 32px gap
  </Tab>
</Tabs>

## Post Card Effects

Each post card includes multiple interactive effects:

<CardGroup cols={2}>
  <Card title="Image Zoom" icon="magnifying-glass-plus">
    Image scales to 110% on hover
  </Card>
  <Card title="Dark Overlay" icon="circle-half-stroke">
    Black overlay fades in on hover
  </Card>
  <Card title="Instagram Icon" icon="instagram">
    Icon appears centered on hover
  </Card>
  <Card title="Zoom Animation" icon="maximize">
    Card zooms in on scroll entry
  </Card>
</CardGroup>

### Hover State

```tsx
// Image zoom
className="group-hover:scale-110 transition-transform duration-700"

// Overlay fade in
className="opacity-0 group-hover:opacity-100 transition-opacity"
```

### Image Aspect Ratio

```tsx
className="aspect-square"
```

All images maintain a 1:1 square aspect ratio for consistent grid layout.

## Social Media Links

Below the grid, social handle links are displayed:

```tsx
<div className="mt-16 flex justify-center gap-8" data-aos="fade-up">
  <a
    href="https://www.facebook.com/share/1Ab4s8pTZ7/"
    target="_blank"
    rel="noopener noreferrer"
    className="flex items-center gap-2 text-sm font-bold 
               uppercase tracking-widest hover:text-[#D4AF37] 
               transition-colors"
  >
    <FontAwesomeIcon icon={faFacebook} className="text-lg" /> 
    @jasperpetshop
  </a>
  
  <a
    href="https://www.instagram.com/invites/contact/?i=1vlgoav5whto1&utm_content=myftgi8"
    target="_blank"
    rel="noopener noreferrer"
    className="flex items-center gap-2 text-sm font-bold 
               uppercase tracking-widest hover:text-[#D4AF37] 
               transition-colors"
  >
    <FontAwesomeIcon icon={faInstagram} className="text-lg" /> 
    @jasperpetshop
  </a>
</div>
```

<Info>
  Both links transition to gold (`#D4AF37`) on hover for brand consistency.
</Info>

## Animations

### Header Animation

```tsx
data-aos="fade-up"
```

Header fades up when scrolled into view.

### Post Grid Animation

```tsx
data-aos="zoom-in"
data-aos-delay={i * 100}
```

Each post zooms in with a staggered delay:
- Post 1: 0ms
- Post 2: 100ms
- Post 3: 200ms
- Post 4: 300ms

### Social Links Animation

```tsx
data-aos="fade-up"
```

Links fade up after the grid is visible.

## Styling

### Colors

<ParamField path="Primary Gold" type="color">
  `#D4AF37` - Badge text and link hover state
</ParamField>

<ParamField path="Overlay" type="color">
  `bg-black/40` - 40% opacity black overlay on hover
</ParamField>

<ParamField path="Text" type="color">
  - Primary: Black
  - Secondary: `gray-500`
</ParamField>

### Border Radius

```tsx
rounded-[32px]
```

Large 32px radius on image cards for smooth, modern look.

### Shadow

```tsx
shadow-lg
```

Large shadow on cards for depth and elevation.

## Responsive Behavior

<CardGroup cols={2}>
  <Card title="Mobile View">
    - 2 column grid
    - Smaller gaps (16px)
    - Touch-optimized sizing
  </Card>
  <Card title="Desktop View">
    - 4 column grid
    - Larger gaps (32px)
    - Hover effects active
  </Card>
</CardGroup>

## External Links

All social links open in new tabs:

```tsx
target="_blank"
rel="noopener noreferrer"
```

<Warning>
  Always use `rel="noopener noreferrer"` with `target="_blank"` for security.
</Warning>

## Image Loading

Unsplash images are optimized with URL parameters:

- `auto=format` - Automatic format selection (WebP when supported)
- `fit=crop` - Crops to fill container
- `q=80` - 80% quality (good balance)
- `w=400` - 400px width

Plus `referrerPolicy="no-referrer"` for external image loading.

## Section Padding

```tsx
className="py-32 bg-white overflow-hidden"
```

- Vertical padding: `128px` (8rem)
- White background
- `overflow-hidden` for animation containment

## Complete Component

```tsx
export const SocialSection = () => {
  const socialPosts = [/* posts data */];

  return (
    <section className="py-32 bg-white overflow-hidden">
      <div className="max-w-7xl mx-auto px-6">
        {/* Header */}
        <div className="text-center mb-16" data-aos="fade-up">
          {/* Badge, Title, Description */}
        </div>

        {/* Post Grid */}
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-8">
          {/* Post cards with hover effects */}
        </div>

        {/* Social Links */}
        <div className="mt-16 flex justify-center gap-8" 
             data-aos="fade-up">
          {/* Facebook and Instagram links */}
        </div>
      </div>
    </section>
  );
};
```

## Customization

### Adding More Posts

```tsx
const socialPosts = [
  // Existing posts...
  {
    id: 5,
    image: "new-image-url",
    link: "instagram-link"
  },
];
```

### Changing Grid Columns

```tsx
// 3 columns on desktop
className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-8"

// 6 columns on desktop
className="grid grid-cols-3 md:grid-cols-6 gap-3 md:gap-6"
```

## Dependencies

- `@fortawesome/react-fontawesome` - Icon component
- `@fortawesome/free-brands-svg-icons` - `faInstagram`, `faFacebook`
- AOS library - Scroll animations